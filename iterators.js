/**
 * A function that uses an iterator to return a string from character codes stored in an array
 *
 *
 * @param {number[]} charCodesArr [104, 105]
 * @return {string} "hi"
 */

export const convertCharCodesToString = (charCodesArr) => {};

/**
 * A functions that takes in an array of strings with numeric values and returns the sum of all strings that store positive numbers or 0 for an empty array or array with negative values only
 *
 *
 * @param {string[]} numericStringsArr ["67", "-12", "0", "15"]
 * @return {number}  82
 */

export const sumOfPositiveNumbers = (numericStringsArr) => {};

/**
 * A functions that takes in an array of strings and checks if all strings in that array are uppercase (all chars in teh string need to be uppercase).
 * Return true if all uppercase, false if at least one isn't, false for an empty array
 *
 *
 * @param {string[]} numericStringsArr ["apple", "PEAR", "Grape"]
 * @return {boolean}  false
 */
export const allUpperCaseStrings = (stringArr) => {};

/**
 * A functions that takes in an array of numbers and uses REDUCE to return the highest number
 *
 *
 * @param {string[]} numberArr [12, 45, 98, 11]
 * @return {number}  98
 */
export const findLargestNumber = (numberArr) => {};
